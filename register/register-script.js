document.getElementById('student-submit-btn').addEventListener('submit', saveNewStudentData);
document.getElementById('sponsor-submit-btn').addEventListener('submit', saveNewSponsorData);

function saveNewStudentData() {
    //to-do: get information of the new student and save into Database ...
}

function saveNewSponsorData() {
    //to-do: get information of the new sponsor and save into Database ...
}